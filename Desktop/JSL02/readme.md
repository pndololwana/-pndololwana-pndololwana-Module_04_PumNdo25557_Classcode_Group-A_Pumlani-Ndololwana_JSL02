# Kanban Task Management - JSL02

A modern, responsive Kanban Board interface designed to track career development tasks. This project combines a structured HTML/CSS layout with functional JavaScript logic to capture and validate user tasks.

## 🚀 Features
- **Task Validation:** JavaScript logic that ensures users enter correct statuses (`todo`, `doing`, `done`) using `while` loops.
- **Console Reporting:** Dynamically logs completed tasks to the console or displays a motivational message if no tasks are done.
- **Responsive UI:** A professional sidebar and multi-column layout built with CSS Flexbox.
- **Modern Styling:** Integrated Google Fonts (Plus Jakarta Sans) and custom CSS variables for a clean aesthetic.

## 🛠️ Technologies Used
- **HTML5:** Semantic structure for the Kanban board.
- **CSS3:** Flexbox layout, custom properties (variables), and responsive design.
- **JavaScript (ES6):** Prompt-based data entry and input validation logic.

## 📂 Files included
- `index.html`: The main dashboard structure.
- `styles.css`: Custom styling and layout rules.
- `scripts.js`: Logic for task input and status checking.
- `favicon.svg` & `logo-light.svg`: Branding assets.

